name: conventional-commit

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-24.04
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Create commitlint config
        run: |
          cat > commitlint.config.mjs << 'EOF'
          export default {
            extends: ['@commitlint/config-conventional'],
            rules: {
              'type-enum': [2, 'always', ['feat', 'feature', 'fix', 'docs', 'style', 'refactor', 'perf', 'test', 'build', 'ci', 'chore', 'revert']],
              'subject-case': [2, 'never', ['sentence-case', 'start-case', 'pascal-case', 'upper-case']],
            // Dependabot and Renovate paste release notes and URLs into the body; the 100-character
            // limit from config-conventional rejects their commits and nobody writes those bodies.
            'body-max-line-length': [0],
            'footer-max-line-length': [0],
            },
          };
          EOF

      - name: Validate Commits
        uses: wagoid/commitlint-github-action@v6
        
       
